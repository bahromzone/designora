name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  backend:
    name: Backend (lint + test)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v5
      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"
          cache: pip
      - name: Install dependencies
        run: pip install -r requirements-dev.txt
      - name: Ruff (lint)
        run: ruff check . --output-format github
      - name: Black (format check)
        shell: bash
        run: |
          output=$(black --check . 2>&1) || status=$?
          echo "$output"
          echo "$output" | sed -n 's/^would reformat \(.*\)$/\1/p' | while IFS= read -r file; do
            rel=${file#"$PWD"/}
            echo "::error file=backend/$rel::Black formatting required"
          done
          exit ${status:-0}
      - name: Pytest (+ coverage)
        env:
          ENVIRONMENT: development
          DATABASE_URL: "sqlite+pysqlite:///:memory:"
          SECRET_KEY: ci-secret
          SESSION_SECRET_KEY: ci-session
          JWT_SECRET_KEY: ci-jwt
          MAIL_USERNAME: ci@example.com
          MAIL_PASSWORD: ci-pass
          MAIL_FROM: ci@example.com
          MAIL_PORT: "587"
          MAIL_SERVER: smtp.example.com
          RECAPTCHA_SECRET_KEY: ci-recaptcha
        run: pytest
      - name: Upload coverage artifact
        uses: actions/upload-artifact@v4
        with:
          name: backend-coverage
          path: backend/coverage.xml
          if-no-files-found: ignore

  frontend:
    name: Frontend (lint + test + build)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v5
      - name: Set up Node
        uses: actions/setup-node@v5
        with:
          node-version: "24"
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - name: Install dependencies
        run: npm ci
      - name: ESLint
        run: npm run lint
      - name: Prettier (format check)
        shell: bash
        run: |
          files=$(npx prettier --list-different "src/**/*.{js,jsx,css}") || status=$?
          if [ -n "$files" ]; then
            while IFS= read -r file; do
              echo "::error file=frontend/$file::Prettier formatting required"
            done <<< "$files"
          fi
          exit ${status:-0}
      - name: Vitest
        run: npm run test:run
      - name: Build
        run: npm run build

  docker:
    name: Docker build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Build backend image
        run: docker build -t designora-backend ./backend
      - name: Build frontend image
        run: docker build -t designora-frontend ./frontend
