name: Frontend diagnostics

on:
  push:
    branches:
      - fix/ci-1167-fastapi-prettier

permissions:
  contents: write

jobs:
  diagnostics:
    name: Capture final frontend diagnostics
    runs-on: ubuntu-latest
    if: github.actor != 'github-actions[bot]'
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/ci-1167-fastapi-prettier
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - name: Run frontend checks
        working-directory: frontend
        run: |
          npm ci
          {
            npm run lint
            npm run format:check
            npm run test:run
            npm run coverage
            npm run build
          } > ../final-frontend-diagnostics.txt 2>&1 || true
      - name: Commit diagnostics
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add final-frontend-diagnostics.txt
          git commit -m "chore(ci): capture final frontend diagnostics"
          git push origin HEAD:fix/ci-1167-fastapi-prettier
