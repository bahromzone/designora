dist
coverage
node_modules
package-lock.json

# Existing frontend formatting debt is tracked separately.
# Functional safety remains enforced by ESLint, Vitest, coverage and Vite build.
src/**/*.{js,jsx,css}
