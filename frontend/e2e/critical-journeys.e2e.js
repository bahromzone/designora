import { test, expect } from "@playwright/test";
test("public learner routes load in a real browser",async({page})=>{await page.route("**/api/courses",route=>route.fulfill({json:[]}));for(const path of["/","/kurslar","/verify/not-found"]){await page.goto(path);await expect(page.locator("#root")).toBeVisible();await expect(page.locator("body")).not.toHaveText("")}});test("protected learner and operations routes never render blank",async({page})=>{for(const path of["/kurslarim","/organish/1","/admin","/admin/moderation","/admin/support"]){await page.goto(path);await expect(page.locator("#root")).toBeVisible();await expect(page.locator("body")).not.toHaveText("")}});
