export function calendarRange(anchor, view){const date=new Date(anchor);const start=new Date(date);if(view==="day"){start.setHours(0,0,0,0);}else if(view==="week"){const day=(start.getDay()+6)%7;start.setDate(start.getDate()-day);start.setHours(0,0,0,0);}else{start.setDate(1);start.setHours(0,0,0,0);}const end=new Date(start);if(view==="day")end.setDate(end.getDate()+1);else if(view==="week")end.setDate(end.getDate()+7);else end.setMonth(end.getMonth()+1);return{start,end};}
export function groupEvents(events){return events.reduce((groups,item)=>{const key=item.starts_at.slice(0,10);groups[key]=[...(groups[key]||[]),item];return groups;},{});}
