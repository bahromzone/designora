import{offlineStore}from"./offlineStore";const API=import.meta.env.VITE_API_URL??"http://127.0.0.1:8000";export async function syncOffline(token){const mutations=await offlineStore.pending();if(!mutations.length)return{synced:0,conflicts:0};const r=await fetch(`${API}/api/offline-sync/batch`,{method:"POST",headers:{"Content-Type":"application/json",Authorization:`Bearer ${token}`},body:JSON.stringify({mutations})});if(!r.ok)throw new Error("Offline sync bajarilmadi");const data=await r.json();for(const item of data.results){if(item.status==="applied"||item.duplicate)await offlineStore.remove(item.client_id)}return data}export function registerOfflineSync(token,onResult){const run=()=>syncOffline(token).then(onResult).catch(()=>{});window.addEventListener("online",run);run();return()=>window.removeEventListener("online",run)}
