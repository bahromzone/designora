const API_URL = import.meta.env.VITE_API_URL ?? "http://127.0.0.1:8000";
async function request(path, token, options = {}) { const response = await fetch(`${API_URL}${path}`, { ...options, credentials: "include", headers: { "Content-Type": "application/json", Authorization: `Bearer ${token}`, ...(options.headers || {}) } }); const data = await response.json().catch(() => ({})); if (!response.ok) throw new Error(data.detail || "Support so‘rovi bajarilmadi"); return data; }
export const supportApi = { search: (token, q) => request(`/api/support/users/search?q=${encodeURIComponent(q)}`, token), workspace: (token, id) => request(`/api/support/users/${id}`, token), regenerate: (token, userId, certificateId) => request(`/api/support/users/${userId}/certificates/${certificateId}/regenerate`, token, { method: "POST" }), resend: (token, userId, notificationId) => request(`/api/support/users/${userId}/notifications/${notificationId}/resend`, token, { method: "POST" }) };
