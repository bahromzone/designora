import {useEffect,useState} from "react";import{useNavigate,useParams}from"react-router-dom";import{useAuth}from"../context/AuthContext";import{checkoutApi}from"../lib/checkoutApi";import"./CheckoutPage.css";
const money=(v)=>`${Number(v||0).toLocaleString("uz-UZ")} so'm`;
export default function CheckoutPage(){const{courseId}=useParams();const{token}=useAuth();const navigate=useNavigate();const[quote,setQuote]=useState(null);const[coupon,setCoupon]=useState("");const[provider,setProvider]=useState("payme");const[busy,setBusy]=useState(false);const[error,setError]=useState("");const load=async(code="")=>{try{setQuote(await checkoutApi.quote(courseId,code));setError("")}catch(e){setError(e.message)}};useEffect(()=>{load()},[courseId]);const pay=async()=>{setBusy(true);try{const result=await checkoutApi.checkout({course_id:Number(courseId),provider,coupon_code:coupon||null},token);window.location.assign(result.pay_url)}catch(e){setError(e.message);setBusy(false)}};if(!quote)return <main className="checkout-shell">{error||"Narx hisoblanmoqda..."}</main>;return <main className="checkout-shell"><section className="checkout-card"><p>Secure checkout</p><h1>{quote.title}</h1><div className="price-row"><span>Kurs narxi</span><strong>{money(quote.original_amount)}</strong></div><div className="price-row discount"><span>Chegirma</span><strong>-{money(quote.discount)}</strong></div><div className="price-row total"><span>Jami</span><strong>{money(quote.total)}</strong></div><label>Promo code<div className="coupon"><input value={coupon} onChange={e=>setCoupon(e.target.value.toUpperCase())} placeholder="UX20"/><button onClick={()=>load(coupon)}>Qo‘llash</button></div></label><fieldset><legend>To‘lov usuli</legend>{quote.providers.map(item=><label className={`provider ${provider===item?"active":""}`} key={item}><input type="radio" checked={provider===item} onChange={()=>setProvider(item)}/><strong>{item.toUpperCase()}</strong><span>{item==="payme"?"Payme ilovasi yoki karta":"Click orqali xavfsiz to‘lov"}</span></label>)}</fieldset>{error&&<p className="checkout-error">{error}</p>}<button className="pay-button" disabled={busy} onClick={pay}>{busy?"Buyurtma yaratilmoqda...":`${money(quote.total)} to‘lash`}</button><small>Ikki marta bosish duplicate buyurtma yaratmaydi. To‘lov holatini natija sahifasida kuzatasiz.</small></section></main>}
