import{useCallback,useEffect,useState}from"react";import{Link,useParams}from"react-router-dom";import{useAuth}from"../context/AuthContext";import{courseCommunityApi}from"../lib/courseCommunityApi";import"./CourseCommunityPage.css";export default function CourseCommunityPage(){const{courseId,threadId}=useParams();const{token}=useAuth();const[list,setList]=useState([]);const[thread,setThread]=useState(null);const[text,setText]=useState("");const[title,setTitle]=useState("");const load=useCallback(async()=>{if(threadId)setThread(await courseCommunityApi.detail(threadId,token));else setList(await courseCommunityApi.list(courseId,token))},[courseId,threadId,token]);useEffect(()=>{load()},[load]);const create=async()=>{await courseCommunityApi.create({course_id:Number(courseId),title,body:text},token);setTitle("");setText("");load()};const reply=async()=>{await courseCommunityApi.reply(threadId,{body:text},token);setText("");load()};if(threadId&&thread)return <main className="cc-shell"><Link to={`/community/${thread.course_id}`}>← Community</Link><article className="cc-topic"><h1>{thread.title}</h1><p>{thread.body}</p></article>{thread.posts.map(p=><article className={`cc-post ${p.is_accepted?"accepted":""}`} key={p.id}><header><strong>{p.author}</strong>{p.is_instructor&&<span>INSTRUCTOR</span>}{p.is_accepted&&<em>✓ Accepted</em>}</header><p>{p.body}</p>{thread.can_accept&&!p.is_accepted&&<button onClick={()=>courseCommunityApi.accept(thread.id,p.id,token).then(load)}>Accept answer</button>}<button onClick={()=>courseCommunityApi.report({post_id:p.id,reason:"inappropriate"},token)}>Report</button></article>)}<textarea value={text} onChange={e=>setText(e.target.value)} placeholder="Reply, @email bilan mention qiling"/><button className="cc-primary" onClick={reply}>Javob yuborish</button><aside><h3>Related threads</h3>{thread.related.map(r=><Link key={r.id} to={`/community/${thread.course_id}/thread/${r.id}`}>{r.title}</Link>)}</aside></main>;return <main className="cc-shell"><h1>Course community</h1><section className="cc-create"><input value={title} onChange={e=>setTitle(e.target.value)} placeholder="Mavzu"/><textarea value={text} onChange={e=>setText(e.target.value)} placeholder="Savol yoki discussion"/><button className="cc-primary" onClick={create}>Mavzu yaratish</button></section>{list.map(t=><Link className="cc-thread" key={t.id} to={`/community/${courseId}/thread/${t.id}`}><strong>{t.title}</strong><span>{t.replies} javob {t.accepted?"· yechilgan":""}</span></Link>)}</main>}
