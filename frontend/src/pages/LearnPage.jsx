import { useCallback, useEffect, useMemo, useState } from "react";
import { Link, useParams } from "react-router-dom";
import AssignmentSection from "../components/AssignmentSection";import LessonSidebar from "../components/LessonSidebar";import NotesSection from "../components/NotesSection";import QASection from "../components/QASection";import VideoPlayer from "../components/VideoPlayer";import { useAuth } from "../context/AuthContext";import { learningApi, quizApi } from "../lib/api";import { assignmentsApi } from "../lib/assignmentsApi";
export default function LearnPage(){const{courseId}=useParams();const{token}=useAuth();const[data,setData]=useState(null);const[assignments,setAssignments]=useState([]);const[quizzes,setQuizzes]=useState([]);const[loading,setLoading]=useState(true);const[error,setError]=useState("");const[activeId,setActiveId]=useState(null);const[marking,setMarking]=useState(false);const load=useCallback(async()=>{if(!token)return;setError("");try{const[course,assignmentRows,quizRows]=await Promise.all([learningApi.learn(courseId,token),assignmentsApi.forCourse(courseId,token).catch(()=>[]),quizApi.courseQuizzes(courseId,token).catch(()=>[])]);setData(course);setAssignments(assignmentRows);setQuizzes(quizRows)}catch(reason){setError(reason.message)}finally{setLoading(false)}},[courseId,token]);useEffect(()=>{setLoading(true);load()},[load]);const flatLessons=useMemo(()=>data?(data.modules||[]).flatMap(module=>module.lessons||[]):[],[data]);useEffect(()=>{if(!data||activeId!==null)return;const first=flatLessons.find(lesson=>!lesson.is_locked&&!lesson.is_completed)||flatLessons.find(lesson=>!lesson.is_locked);setActiveId(first?.id??flatLessons[0]?.id??null)},[data,flatLessons,activeId]);const activeLesson=flatLessons.find(lesson=>lesson.id===activeId)||null;async function toggleComplete(lesson){if(!lesson||lesson.is_locked)return;setMarking(true);try{if(lesson.is_completed)await learningApi.uncompleteLesson(lesson.id,token);else await learningApi.completeLesson(lesson.id,token);await load()}catch(reason){setError(reason.message)}finally{setMarking(false)}}if(loading)return <main className="container py-16" aria-busy="true">Dars yuklanmoqda...</main>;if(error&&!data)return <main className="container py-16"><p role="alert">{error}</p><button onClick={load}>Qayta urinish</button></main>;if(!data)return null;if(!data.is_enrolled)return <main className="container py-16"><h1>Bu kursga hali yozilmagansiz</h1><Link to={`/kurslar/${courseId}`}>Kurs sahifasiga o‘tish</Link></main>;return <main className="learn-page"><Link to="/kurslarim">← {data.title}</Link>{error&&<p role="alert">{error}</p>}<div className="learn-layout"><LessonSidebar modules={data.modules||[]} activeId={activeId} onSelect={setActiveId}/><section>{activeLesson&&<><VideoPlayer lessonId={activeLesson.id} token={token} src={activeLesson.video_url} storageKey={`lesson-${activeLesson.id}`} poster={activeLesson.poster_url} subtitles={activeLesson.subtitles||[]} transcript={activeLesson.transcript||""} title={activeLesson.title} onEnded={()=>!activeLesson.is_completed&&toggleComplete(activeLesson)}/><h1>{activeLesson.title}</h1>{activeLesson.description&&<p>{activeLesson.description}</p>}{activeLesson.content&&<div>{activeLesson.content}</div>}<button onClick={()=>toggleComplete(activeLesson)} disabled={marking}>{activeLesson.is_completed?"✓ Tugatilgan (bekor qilish)":"Tugatilgan deb belgilash"}</button><QASection lessonId={activeLesson.id}/><NotesSection lessonId={activeLesson.id}/></>}</section></div><AssignmentSection courseId={Number(courseId)} assignments={assignments}/>{quizzes.length>0&&<p>{quizzes.length} ta quiz mavjud</p>}</main>}
