import{useEffect,useState}from"react";import{useAuth}from"../context/AuthContext";import{monetizationApi}from"../lib/monetizationApi";import"./PricingPage.css";const money=v=>`${Number(v||0).toLocaleString("uz-UZ")} so‘m`;export default function PricingPage(){const{token}=useAuth();const[data,setData]=useState(null);const[msg,setMsg]=useState("");useEffect(()=>{monetizationApi.catalog().then(setData)},[]);if(!data)return <main className="pricing-shell">Yuklanmoqda...</main>;const act=async(fn)=>{try{const r=await fn();setMsg(r.message)}catch(e){setMsg(e.message)}};return <main className="pricing-shell"><header><p>Roadmap 3.23</p><h1>O‘rganish usulingizni tanlang</h1><span>Alohida kursdan boshlang. Bundle qiymat beradi, subscription esa faqat kontent va retention tayyor bo‘lganda ochiladi.</span></header>{msg&&<div className="pricing-msg">{msg}</div>}<section><h2>Course bundles</h2><div className="pricing-grid">{data.bundles.map(b=><article key={b.id}><small>BUNDLE</small><h3>{b.title}</h3><p>{b.description}</p><strong>{money(b.price)}</strong><button onClick={()=>act(()=>monetizationApi.bundle(b.id,token))}>Bundle olish</button></article>)}{!data.bundles.length&&<p>Aktiv bundle hali yo‘q.</p>}</div></section><section><h2>Monthly subscription</h2><p>{data.subscription_warning}</p><div className="pricing-grid">{data.subscriptions.map(p=><article key={p.id}><small>MONTHLY</small><h3>{p.name}</h3><strong>{money(p.monthly_price)}/oy</strong><button onClick={()=>act(()=>monetizationApi.subscribe(p.id,token))}>Obuna bo‘lish</button></article>)}{!data.subscriptions.length&&<p>Subscription hozircha yopiq. Bu to‘g‘ri qaror, fake recurring revenue kerak emas.</p>}</div></section><section className="pricing-aid"><h2>Moliyaviy yordam</h2><p>Scholarship yoki bo‘lib to‘lash uchun kurs sahifasidan ariza yuborish mumkin.</p></section></main>}
