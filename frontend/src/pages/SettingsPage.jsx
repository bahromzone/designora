// prettier-ignore-start
import { useEffect, useState } from "react";
import { useAuth } from "../context/AuthContext";
import { accountApi } from "../lib/accountApi";
export default function SettingsPage() { const { user } = useAuth(); const [form, setForm] = useState({ name: "", bio: "", phone: "", location: "", website: "" }); const [passwords, setPasswords] = useState({ current_password: "", new_password: "" }); const [message, setMessage] = useState(""); const [error, setError] = useState(""); const [saving, setSaving] = useState(false); const [changing, setChanging] = useState(false); useEffect(() => { accountApi.profile().then((profile) => setForm({ name: profile.name || "", bio: profile.bio || "", phone: profile.phone || "", location: profile.location || "", website: profile.website || "" })).catch((e) => setError(e.message)); }, []); function setField(key, value) { setForm((current) => ({ ...current, [key]: value })); } async function saveProfile(event) { event.preventDefault(); setSaving(true); setMessage(""); setError(""); try { await accountApi.updateProfile(form); setMessage("Profil ma’lumotlari saqlandi."); } catch (e) { setError(e.message); } finally { setSaving(false); } } async function changePassword(event) { event.preventDefault(); setChanging(true); setMessage(""); setError(""); try { await accountApi.changePassword(passwords); setPasswords({ current_password: "", new_password: "" }); setMessage("Parol muvaffaqiyatli o‘zgartirildi."); } catch (e) { setError(e.message); } finally { setChanging(false); } } return <section className="shell py-16 sm:py-20"><p className="label">Hisobingizni boshqaring</p><h1 className="mt-3 font-serif text-4xl font-semibold text-ink">Sozlamalar</h1><p className="mt-3 max-w-2xl text-base leading-7" style={{ color: "var(--muted)" }}>Shaxsiy ma’lumotlar va kirish xavfsizligini shu yerdan boshqaring.</p>{message && <p className="mt-8 rounded-xl bg-emerald-50 px-4 py-3 text-sm text-emerald-700">{message}</p>}{error && <p className="mt-8 rounded-xl bg-red-50 px-4 py-3 text-sm text-red-700">{error}</p>}<div className="mt-10 grid gap-6 lg:grid-cols-2"><form onSubmit={saveProfile} className="rounded-2xl border bg-white p-6" style={{ borderColor: "var(--border)" }}><p className="label">Shaxsiy ma’lumotlar</p><div className="mt-6 space-y-5">{[["name","Ism"],["bio","Bio"],["phone","Telefon"],["location","Joylashuv"],["website","Veb-sayt"]].map(([key,label]) => <label className="block" key={key}><span className="mb-2 block text-sm font-semibold text-ink">{label}</span><input className="input-field" value={form[key]} onChange={(event) => setField(key, event.target.value)} /></label>)}</div><button className="btn-primary mt-6" disabled={saving}>{saving ? "Saqlanmoqda..." : "Saqlash"}</button></form><form onSubmit={changePassword} className="rounded-2xl border bg-white p-6" style={{ borderColor: "var(--border)" }}><p className="label">Kirish xavfsizligi</p><p className="mt-3 text-sm" style={{ color: "var(--muted)" }}>{user?.provider && user.provider !== "local" ? `${user.provider} orqali kirgansiz, parol bu yerda boshqarilmaydi.` : "Yangi parol kamida 8 belgi, katta harf va raqamdan iborat bo‘lsin."}</p><div className="mt-6 space-y-5">{[["current_password","Joriy parol"],["new_password","Yangi parol"]].map(([key,label]) => <label className="block" key={key}><span className="mb-2 block text-sm font-semibold text-ink">{label}</span><input className="input-field" type="password" value={passwords[key]} onChange={(event) => setPasswords((current) => ({ ...current, [key]: event.target.value }))} /></label>)}</div><button className="btn-primary mt-6" disabled={changing || (user?.provider && user.provider !== "local")}>{changing ? "O‘zgartirilmoqda..." : "Parolni yangilash"}</button></form></div></section>; }
// prettier-ignore-end
