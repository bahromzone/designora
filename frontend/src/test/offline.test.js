import{describe,expect,it,vi}from"vitest";vi.mock("../lib/offlineStore",()=>({offlineStore:{pending:vi.fn().mockResolvedValue([]),remove:vi.fn()}}));import{syncOffline}from"../lib/offlineSync";describe("offline sync",()=>{it("skips network when queue empty",async()=>{global.fetch=vi.fn();expect(await syncOffline("t")).toEqual({synced:0,conflicts:0});expect(fetch).not.toHaveBeenCalled()})});
